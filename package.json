{
  "name": "mantis-revamp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "preflight": "node scripts/preflight.mjs",
    "setup": "npm run preflight && docker compose up -d db && node -e \"setTimeout(()=>{},5000)\" && npm run migrate && npm run seed",
    "dev": "npm --prefix apps/web run dev",
    "dev:all": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm --prefix apps/api run dev",
    "dev:web": "npm --prefix apps/web run dev",
    "build": "npm --prefix packages/shared run build && npm --prefix apps/web run build",
    "test": "npm --prefix apps/api test && npm --prefix apps/web test",
    "test:api": "npm --prefix apps/api test",
    "test:web": "npm --prefix apps/web test",
    "test:unit": "npm --prefix apps/api run test:unit",
    "test:integration": "npm --prefix apps/api run test:integration",
    "test:cli": "npm --prefix apps/cli test",
    "bz": "node apps/cli/dist/index.js",
    "mantis": "node apps/cli/dist/index.js",
    "seed": "npm --prefix apps/api run seed",
    "migrate": "npm --prefix apps/api run migrate"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
